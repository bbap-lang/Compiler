using System.Collections.Immutable;
using BBAP.Parser.Expressions;
using BBAP.Results;

namespace BBAP.SyntaxAnalyzer; 

public class SyntaxAnalyzer {
    public Result<byte> Run(ImmutableArray<IExpression> tree) {
        
        
        return Ok<byte>(0);
    }
}